# 🍕 DATAPIZZA-AI PHP FRAMEWORK
# Minimal educational environment configuration
# Every variable here is read by the examples to keep the code clean and readable.

# 🔑 Your API key (required)
# Get one from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# 🕓 Timeout for HTTP requests (in seconds)
# Increase if your connection or device is slow
REQUEST_TIMEOUT=30

# ⚙️ Maximum number of concurrent API calls
# Keep =1 for Raspberry Pi or low-RAM environments
MAX_CONCURRENT_REQUESTS=1

# 💾 Enable caching to reduce API calls
# true = use local cache folder, false = always fetch fresh data
CACHE_ENABLED=true

# 🔍 Default embedding model
# Smaller models are faster and cheaper — perfect for local tests
EMBEDDING_MODEL=text-embedding-3-small

# 💬 Default chat model
# Use “gpt-4o-mini” for speed and cost efficiency
CHAT_MODEL=gpt-4o-mini

# 📂 Optional local folder for RAG examples
# Point this to any folder containing your .txt or .md documents
DOCUMENTS_PATH=./docs

# 🧠 Memory file used by agents
# Keeps track of your conversation context between turns
MEMORY_FILE=memory.json

# 🧰 Debug mode (true/false)
# When enabled, prints intermediate steps, payloads, and reasoning traces
DEBUG_MODE=false
